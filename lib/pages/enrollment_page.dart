import 'package:flutter/material.dart';

class EnrollmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Enrollment'),
      ),
      body: Center(
        child: Text('Enrollment Page'),
      ),
    );
  }
}